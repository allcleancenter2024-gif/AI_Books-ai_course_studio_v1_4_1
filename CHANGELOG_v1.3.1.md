# AI 강의 활용 Studio v1.3.1

## 핵심 수정
- 8765 포트에 예전 Studio가 실행 중이면 버전을 비교합니다.
- 같은 버전이면 기존 서버를 재사용합니다.
- 예전 Studio임이 HTTP `/api/health`와 `/openapi.json`으로 확인되면 Windows에서 해당 포트의 PID만 찾아 종료한 뒤 최신 서버를 시작합니다.
- 다른 프로그램이 8765를 사용 중이면 종료하지 않고 안전하게 중단합니다.
- 서버 준비 완료 판정에 `version == 1.3.1` 조건을 추가했습니다.
- 브라우저를 열 때 버전/시간 쿼리를 붙여 이전 화면 캐시를 피합니다.
- `/`와 `/static/*`에 `Cache-Control: no-store`를 적용했습니다.
- HTML의 버전 표시는 서버에서 주입하고, JS가 `/api/health`의 버전으로 다시 확인합니다.

## 이번 오류의 원인
v1.3.0 실행기가 포트 8765에서 어떤 버전의 Studio든 `/api/health`가 정상이라면 그대로 재사용했습니다. 그래서 v1.3.0 파일을 실행해도 기존 v1.2.2 서버가 살아 있으면 새 서버가 시작되지 않았습니다.
