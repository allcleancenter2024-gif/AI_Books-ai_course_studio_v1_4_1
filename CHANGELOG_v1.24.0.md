# AI Course Studio v1.24.0 [2026-08-29]

## 하이브리드 웹 RAG

- SearXNG 자체 검색을 기본 웹 검색 공급자로 추가했습니다.
- SearXNG는 별도 Compose에서 `127.0.0.1:8888`로만 노출됩니다.
- Firecrawl Cloud를 고품질 웹 보강의 선택형 검색·본문 추출 공급자로 추가했습니다.
- Firecrawl 키가 없거나 401·402·408·429·5xx·월간 상한 상태이면 로컬 추출로 대체합니다.
- Trafilatura와 기존 BeautifulSoup을 결합한 로컬 본문 추출을 적용했습니다.
- SSRF, MIME, 리다이렉트, 응답 크기, robots.txt 제한을 적용했습니다.
- 검색 캐시 24시간, 동시 검색 1건, 근거 URL 최대 5개 제한을 적용했습니다.
- 사용 출처의 제목·기관·확인일·A/B/C 등급을 결과 화면에서 확인할 수 있습니다.

## 호환성

- 내부 문서 RAG, SQLite, Ollama, LM Studio와 기존 교재 생성 API는 유지됩니다.
- DuckDuckGo와 Tavily 공급자는 비활성 호환 경로로 보존됩니다.
- 기존 Docker Compose, 포트, 데이터 볼륨은 변경하지 않습니다.

## 작업 큐 안정성 보강

- 장시간 작업 상태를 SQLite `job_states`에 영구 저장합니다.
- 인증된 사용자가 작업 상태를 조회하고 취소할 수 있는 API를 추가했습니다.
- 교재 생성은 주차 경계에서 취소 상태를 확인하며 취소된 작업을 완료로 보고하지 않습니다.
