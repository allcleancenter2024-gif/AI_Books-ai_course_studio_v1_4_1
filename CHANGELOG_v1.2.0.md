# v1.2.0

## 구조 개편
- 거대한 `app.py`를 얇은 실행 진입점으로 축소
- API / 서비스 / DB / 기준 데이터 모듈 분리
- HTML 내부 CSS·JS 제거, 정적 모듈 파일로 분리
- 기존 Provider와 3단계 JSON 복구 파이프라인 재사용
- 전체 교재 생성도 동일한 서비스 경로 사용

## UI/UX
- 항상 보이는 상단 상태바 추가
- 서버 상태 / Provider / 현재 작업 / 진행률 표시
- 왼쪽 1~6단계 작업 메뉴 추가
- Provider별 색상 구분
- 성공/작업/주의/오류 상태 색상 분리
- Base URL/API Key를 고급 설정으로 접어 기본 화면 단순화
- 긴 페이지에서만 자동으로 나타나는 `위로 가기` 버튼 추가
- 반응형 모바일 레이아웃 강화

## 경량화
- 실행할 때마다 `pip install`하던 동작 제거
- 필요한 패키지가 없을 때만 최초 1회 설치
- React/빌드 도구 없이 Vanilla JS 모듈 유지
