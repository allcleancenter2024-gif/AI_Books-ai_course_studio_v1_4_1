# v1.1.2

## 핵심 수정
1. 프런트엔드 `api()`가 Response body를 `json()` 후 `text()`로 두 번 읽던 버그 수정.
2. 응답 본문은 `text()`로 한 번만 읽고 저장한 문자열을 JSON 파싱.
3. 1주차 AI 생성 작업을 3단계(설명, 프롬프트 10개, 실습 10개)로 분리.
4. 각 단계별 진행 메시지 표시.
5. FastAPI 예상 외 오류도 JSON `{detail: ...}`로 반환.
6. 로컬 모델 단계별 max_tokens 축소.
