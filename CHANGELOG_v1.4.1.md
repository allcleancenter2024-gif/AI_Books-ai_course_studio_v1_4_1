# v1.4.1 · 로컬 AI 연결 및 요약 안정화

- `모델 찾기` 결과가 화면에만 표시되고 서버 설정에는 저장되지 않던 오류 수정
- 요약 시작 전에 선택 Provider의 모델·연결 상태를 자동 확인
- LM Studio/Ollama의 모델이 비어 있으면 모델 목록에서 자동 선택
- 문자열, content 배열, legacy text 등 OpenAI 호환 응답 변형 지원
- Ollama 사고형 모델은 `think=false`로 호출해 내부 추론만 반환되는 문제 방지
- 로컬 모델이 출력 토큰을 내부 추론에 모두 사용해 빈 content를 반환하면 출력량을 늘려 한 번 재시도
- 모델 서버 미실행, 모델 미로딩, 빈 응답을 서로 다른 안내 문구로 표시
- 12,000자 이하 자료는 불필요한 2단계 요약을 생략해 소형 로컬 모델의 원문 누락 방지
